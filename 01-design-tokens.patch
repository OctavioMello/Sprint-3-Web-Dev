diff --git a/src/index.css b/src/index.css
index 4a02e5d..2f35e25 100644
--- a/src/index.css
+++ b/src/index.css
@@ -5,27 +5,38 @@ body {
     margin: 0;
     padding: 0;
     background: var(--ink);
+    scroll-behavior: smooth;
 }
 
 :root {
-    --ink: #0c1220;
-    --surface: #131b2c;
-    --surface-raised: #1a2438;
-
-    --line: rgba(242, 243, 240, 0.1);
-    --line-strong: rgba(242, 243, 240, 0.18);
-
-    --paper: #f2f3f0;
-    --paper-dim: #c7cbd6;
-    --muted: #8a93a6;
-
-    --signal: #ffb454;
-    --signal-dim: rgba(255, 180, 84, 0.16);
-
-    --sense: #5eead4;
-    --sense-dim: rgba(94, 234, 212, 0.14);
-
-    --danger: #ff8a80;
+    /* ---- Base: black & deep blue ---- */
+    --void: #05070d;
+    --ink: #070c17;
+    --surface: #0d1526;
+    --surface-alt: #0a1428;
+    --surface-raised: #131f38;
+
+    --line: rgba(147, 173, 255, 0.09);
+    --line-strong: rgba(147, 173, 255, 0.2);
+
+    --paper: #eef1fb;
+    --paper-dim: #aab4d1;
+    --muted: #6b7694;
+
+    /* ---- Blue accent family (each section leans on one) ---- */
+    --azure: #4c7fff;
+    --azure-dim: rgba(76, 127, 255, 0.16);
+    --cyan: #35c5f0;
+    --cyan-dim: rgba(53, 197, 240, 0.14);
+    --indigo: #8f8bff;
+    --indigo-dim: rgba(143, 139, 255, 0.14);
+    --steel: #7186ab;
+    --steel-dim: rgba(113, 134, 171, 0.14);
+
+    --signal: var(--azure);
+    --signal-dim: var(--azure-dim);
+    --sense: var(--cyan);
+    --sense-dim: var(--cyan-dim);
 
     --font-display: 'Space Grotesk', sans-serif;
     --font-body: 'Inter', sans-serif;
@@ -39,10 +50,33 @@ body {
 }
 
 .vaa {
+    position: relative;
     background: var(--ink);
     color: var(--paper);
     font-family: var(--font-body);
     line-height: 1.5;
+    overflow-x: clip;
+}
+
+/* Fixed atmosphere sitting behind every section: faint blueprint grid + vignette */
+.vaa::before {
+    content: "";
+    position: fixed;
+    inset: 0;
+    z-index: 0;
+    pointer-events: none;
+    background-image:
+        linear-gradient(rgba(147, 173, 255, 0.05) 1px, transparent 1px),
+        linear-gradient(90deg, rgba(147, 173, 255, 0.05) 1px, transparent 1px),
+        radial-gradient(ellipse 60% 45% at 18% 0%, rgba(76, 127, 255, 0.16), transparent 60%),
+        radial-gradient(ellipse 55% 40% at 100% 30%, rgba(53, 197, 240, 0.08), transparent 60%);
+    background-size: 64px 64px, 64px 64px, auto, auto;
+    mask-image: linear-gradient(180deg, black, black 70%, transparent 100%);
+}
+
+.vaa > * {
+    position: relative;
+    z-index: 1;
 }
 
 .vaa h1,
@@ -75,36 +109,40 @@ body {
     display: inline-flex;
     align-items: center;
     gap: 0.5rem;
-    transition: 0.25s;
+    transition: transform 0.25s ease, box-shadow 0.25s ease, border-color 0.25s ease, color 0.25s ease;
 }
 
 .btn-primary {
-    background: var(--signal);
-    color: #241503;
+    background: linear-gradient(135deg, var(--azure), #2f5fe0);
+    color: #eef4ff;
+    box-shadow: 0 14px 30px -14px rgba(76, 127, 255, 0.7);
 }
 
 .btn-primary:hover {
     transform: translateY(-2px);
+    box-shadow: 0 18px 36px -14px rgba(76, 127, 255, 0.85);
 }
 
 .btn-primary:disabled {
     opacity: 0.7;
     cursor: progress;
     transform: none;
+    box-shadow: none;
 }
 
 .btn-ghost {
-    background: transparent;
+    background: rgba(238, 241, 251, 0.03);
     color: var(--paper);
     border: 1px solid var(--line-strong);
 }
 
 .btn-ghost:hover {
-    border-color: var(--sense);
-    color: var(--sense);
+    border-color: var(--cyan);
+    color: var(--cyan);
 }
 
 .section {
+    position: relative;
     padding: clamp(3.5rem, 8vw, 6rem) var(--edge);
 }
 
@@ -121,7 +159,7 @@ body {
     font-size: 0.7rem;
     letter-spacing: 0.16em;
     text-transform: uppercase;
-    color: var(--sense);
+    color: var(--cyan);
     margin-bottom: 0.8rem;
 }
 
@@ -130,8 +168,8 @@ body {
     width: 6px;
     height: 6px;
     border-radius: 50%;
-    background: var(--sense);
-    box-shadow: 0 0 0 4px var(--sense-dim);
+    background: var(--cyan);
+    box-shadow: 0 0 0 4px var(--cyan-dim);
 }
 
 .section-title {
@@ -144,4 +182,66 @@ body {
     margin-top: 1rem;
     color: var(--paper-dim);
     max-width: 38rem;
-}
\ No newline at end of file
+}
+
+/* ---------------------------------------------------------------- */
+/* Scroll-reveal transition system, shared by every section         */
+/* ---------------------------------------------------------------- */
+
+.reveal {
+    opacity: 0;
+    transform: translateY(30px);
+    transition:
+        opacity 0.8s cubic-bezier(0.16, 0.8, 0.24, 1),
+        transform 0.8s cubic-bezier(0.16, 0.8, 0.24, 1);
+    will-change: opacity, transform;
+}
+
+.reveal-visible {
+    opacity: 1;
+    transform: translateY(0);
+}
+
+.reveal-left {
+    transform: translateX(-36px);
+}
+
+.reveal-left.reveal-visible {
+    transform: translateX(0);
+}
+
+.reveal-right {
+    transform: translateX(36px);
+}
+
+.reveal-right.reveal-visible {
+    transform: translateX(0);
+}
+
+.reveal-scale {
+    transform: scale(0.92);
+}
+
+.reveal-scale.reveal-visible {
+    transform: scale(1);
+}
+
+.reveal-fade {
+    transform: none;
+}
+
+@media (prefers-reduced-motion: reduce) {
+    html, body {
+        scroll-behavior: auto;
+    }
+
+    .reveal,
+    .reveal-left,
+    .reveal-right,
+    .reveal-scale,
+    .reveal-fade {
+        transition: none !important;
+        opacity: 1 !important;
+        transform: none !important;
+    }
+}
