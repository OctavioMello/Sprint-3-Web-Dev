diff --git a/src/components/Hero/Hero.css b/src/components/Hero/Hero.css
index 54a2295..9025c2b 100644
--- a/src/components/Hero/Hero.css
+++ b/src/components/Hero/Hero.css
@@ -1,183 +1,202 @@
 .hero {
     position: relative;
     overflow: hidden;
+    isolation: isolate;
 
-    padding:
-        clamp(3.5rem, 10vw, 6rem)
-        var(--edge)
-        clamp(3rem, 6vw, 4.5rem);
+    padding: clamp(4rem, 12vw, 7rem) var(--edge) clamp(3.5rem, 7vw, 5rem);
 
     display: flex;
     flex-direction: column;
+    gap: 3.5rem;
 
-    gap: 3rem;
+    background: radial-gradient(ellipse 90% 70% at 50% -10%, rgba(76, 127, 255, 0.14), transparent 65%), var(--void);
+}
+
+.hero-grid {
+    position: absolute;
+    inset: -10% -10%;
+    z-index: 0;
+    pointer-events: none;
+    background-image:
+        linear-gradient(rgba(147, 173, 255, 0.06) 1px, transparent 1px),
+        linear-gradient(90deg, rgba(147, 173, 255, 0.06) 1px, transparent 1px);
+    background-size: 46px 46px;
+    mask-image: radial-gradient(ellipse 70% 60% at 50% 20%, black, transparent 75%);
+}
+
+.hero-glow {
+    position: absolute;
+    top: -18%;
+    right: -14%;
+    width: 34rem;
+    height: 34rem;
+    border-radius: 50%;
+    background: radial-gradient(circle, rgba(53, 197, 240, 0.16), transparent 68%);
+    filter: blur(6px);
+    pointer-events: none;
+    z-index: 0;
+}
+
+.hero-content,
+.hero-flow {
+    position: relative;
+    z-index: 1;
+}
+
+.hero-in {
+    opacity: 0;
+    transform: translateY(18px);
+    transition: opacity 0.7s cubic-bezier(0.16, 0.8, 0.24, 1), transform 0.7s cubic-bezier(0.16, 0.8, 0.24, 1);
+}
+
+.hero.is-loaded .hero-in {
+    opacity: 1;
+    transform: translateY(0);
+}
+
+.hero-content {
+    max-width: 40rem;
 }
 
 .hero-title {
     font-size: clamp(2.1rem, 8vw, 3.6rem);
-
     font-weight: 700;
-
     margin-top: 0.7rem;
-
     line-height: 1;
 }
 
 .hero-title span {
-    color: var(--signal);
+    background: linear-gradient(120deg, var(--azure), var(--cyan));
+    -webkit-background-clip: text;
+    background-clip: text;
+    color: transparent;
 }
 
 .hero-claim {
     margin-top: 1.1rem;
-
     font-family: var(--font-display);
-
     font-size: clamp(1.05rem, 3vw, 1.3rem);
-
-    color: var(--sense);
-
+    color: var(--cyan);
     font-weight: 500;
 }
 
 .hero-desc {
     margin-top: 1rem;
-
     color: var(--paper-dim);
-
     max-width: 34rem;
 }
 
 .hero-actions {
     margin-top: 2rem;
-
     display: flex;
     flex-wrap: wrap;
-
     gap: 0.8rem;
 }
 
 .hero-flow {
     display: flex;
-
     flex-direction: column;
-
     align-items: center;
-
-    gap: 2rem;
+    gap: 2.2rem;
 }
 
-.hero-pulse {
+.hero-radar {
     position: relative;
-
-    width: 170px;
-    height: 170px;
-
+    width: 190px;
+    height: 190px;
     display: flex;
-
     align-items: center;
     justify-content: center;
+    border-radius: 50%;
+    border: 1px solid var(--line);
 }
 
-.pulse-core {
-    position: relative;
-    z-index: 2;
-
-    width: 72px;
-    height: 72px;
-
+.radar-ring {
+    position: absolute;
     border-radius: 50%;
-
-    background: var(--surface-raised);
-
     border: 1px solid var(--line-strong);
-
-    display: flex;
-
-    align-items: center;
-    justify-content: center;
-
-    color: var(--signal);
-
-    font-size: 0.85rem;
 }
 
-.ring {
-    position: absolute;
+.radar-ring.r1 { inset: 18%; }
+.radar-ring.r2 { inset: 36%; }
+.radar-ring.r3 { inset: 54%; border-color: var(--cyan); opacity: 0.5; }
 
+.radar-sweep {
+    position: absolute;
+    inset: 0;
     border-radius: 50%;
-
-    border: 1px solid var(--sense);
-
-    opacity: 0;
-
-    animation: pulse 3.6s ease-out infinite;
-}
-
-.ring2 {
-    animation-delay: 1.2s;
+    background: conic-gradient(from 0deg, rgba(53, 197, 240, 0.5), transparent 26%);
+    animation: sweep 3.4s linear infinite;
+    mix-blend-mode: screen;
 }
 
-.ring3 {
-    animation-delay: 2.4s;
+@keyframes sweep {
+    to { transform: rotate(360deg); }
 }
 
-@keyframes pulse {
-    0% {
-        width: 72px;
-        height: 72px;
-        opacity: 0.55;
-    }
-
-    100% {
-        width: 170px;
-        height: 170px;
-        opacity: 0;
-    }
+.radar-core {
+    position: relative;
+    z-index: 2;
+    width: 68px;
+    height: 68px;
+    border-radius: 50%;
+    background: var(--surface-raised);
+    border: 1px solid var(--line-strong);
+    display: flex;
+    align-items: center;
+    justify-content: center;
+    color: var(--azure);
+    font-size: 0.85rem;
+    box-shadow: 0 0 24px -4px rgba(76, 127, 255, 0.55);
 }
 
 .hero-steps {
     list-style: none;
-
     margin: 0;
     padding: 0;
-
     display: flex;
-
     flex-wrap: wrap;
-
     justify-content: center;
-
     gap: 0.5rem;
-
     max-width: 28rem;
 }
 
 .hero-step {
     display: flex;
-
     align-items: center;
-
     gap: 0.5rem;
-
     font-size: 0.8rem;
-
     color: var(--paper-dim);
-
-    background: var(--surface);
-
+    background: rgba(238, 241, 251, 0.03);
     border: 1px solid var(--line);
-
     border-radius: 999px;
+    padding: 0.45rem 0.8rem 0.45rem 0.5rem;
+    transition: border-color 0.25s ease, transform 0.25s ease;
+}
 
-    padding:
-        0.45rem
-        0.8rem
-        0.45rem
-        0.5rem;
+.hero-step:hover {
+    border-color: var(--cyan);
+    transform: translateY(-2px);
 }
 
 .hero-step-i {
-    color: var(--signal);
-
+    color: var(--azure);
     font-size: 0.65rem;
-}
\ No newline at end of file
+}
+
+.hero-step-arrow {
+    color: var(--muted);
+}
+
+@media (min-width: 900px) {
+    .hero {
+        flex-direction: row;
+        align-items: center;
+        justify-content: space-between;
+        gap: 2rem;
+    }
+
+    .hero-flow {
+        flex-shrink: 0;
+    }
+}
diff --git a/src/components/Hero/Hero.jsx b/src/components/Hero/Hero.jsx
index fa7ad08..0d837d7 100644
--- a/src/components/Hero/Hero.jsx
+++ b/src/components/Hero/Hero.jsx
@@ -1,116 +1,75 @@
+import { useEffect, useState } from "react";
 import "./Hero.css";
 
-const FLOW = [
-    "Câmera",
-    "IA",
-    "Informação",
-    "Usuário decide",
-];
+const FLOW = ["Câmera", "IA", "Informação", "Usuário decide"];
 
 function Hero() {
+    const [loaded, setLoaded] = useState(false);
+
+    useEffect(() => {
+        const id = requestAnimationFrame(() => setLoaded(true));
+        return () => cancelAnimationFrame(id);
+    }, []);
+
     return (
-        <section className="hero" id="top">
+        <section className={`hero ${loaded ? "is-loaded" : ""}`} id="top">
+            <div className="hero-grid" aria-hidden="true" />
+            <div className="hero-glow" aria-hidden="true" />
 
-            <div>
-                <p className="eyebrow">
+            <div className="hero-content">
+                <p className="eyebrow hero-in" style={{ transitionDelay: "60ms" }}>
                     Protótipo acadêmico · assistência de percepção
                 </p>
 
-                <h1 className="hero-title">
+                <h1 className="hero-title hero-in" style={{ transitionDelay: "140ms" }}>
                     VISION <span>ASSIST</span> AI
                 </h1>
 
-                <p className="hero-claim">
+                <p className="hero-claim hero-in" style={{ transitionDelay: "220ms" }}>
                     Informação visual transformada em autonomia.
                 </p>
 
-                <p className="hero-desc">
+                <p className="hero-desc hero-in" style={{ transitionDelay: "300ms" }}>
                     Um assistente de percepção para estudantes universitários
                     com deficiência visual. A câmera do smartphone interpreta
                     o ambiente ao redor e devolve orientações sonoras curtas,
                     no momento em que elas importam.
                 </p>
 
-                <div className="hero-actions">
-                    <a
-                        href="#problema"
-                        className="btn btn-primary"
-                    >
+                <div className="hero-actions hero-in" style={{ transitionDelay: "380ms" }}>
+                    <a href="#problema" className="btn btn-primary">
                         Conhecer a solução
                     </a>
 
-                    <a
-                        href="#demo"
-                        className="btn btn-ghost"
-                    >
+                    <a href="#demo" className="btn btn-ghost">
                         Ver protótipo simulado
                     </a>
                 </div>
             </div>
 
-            <div className="hero-flow">
-
-                <div className="hero-pulse">
-                    <span
-                        className="ring"
-                        style={{
-                            width: 72,
-                            height: 72
-                        }}
-                    />
-
-                    <span
-                        className="ring ring2"
-                        style={{
-                            width: 72,
-                            height: 72
-                        }}
-                    />
-
-                    <span
-                        className="ring ring3"
-                        style={{
-                            width: 72,
-                            height: 72
-                        }}
-                    />
-
-                    <span className="pulse-core mono">
-                        IA
-                    </span>
+            <div className="hero-flow hero-in" style={{ transitionDelay: "260ms" }}>
+                <div className="hero-radar">
+                    <span className="radar-ring r1" />
+                    <span className="radar-ring r2" />
+                    <span className="radar-ring r3" />
+                    <span className="radar-sweep" />
+                    <span className="radar-core mono">IA</span>
                 </div>
 
                 <ol className="hero-steps">
                     {FLOW.map((step, index) => (
-                        <li
-                            key={step}
-                            className="hero-step"
-                        >
-                            <span className="mono hero-step-i">
-                                0{index + 1}
-                            </span>
-
-                            <span>
-                                {step}
-                            </span>
-
+                        <li key={step} className="hero-step">
+                            <span className="mono hero-step-i">0{index + 1}</span>
+                            <span>{step}</span>
                             {index < FLOW.length - 1 && (
-                                <span
-                                    style={{
-                                        color: "var(--muted)"
-                                    }}
-                                >
-                                    →
-                                </span>
+                                <span className="hero-step-arrow">→</span>
                             )}
                         </li>
                     ))}
                 </ol>
-
             </div>
-
         </section>
     );
 }
 
-export default Hero;
\ No newline at end of file
+export default Hero;
